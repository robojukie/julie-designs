# Draft/unpublished pages — kept locally for reference, never pushed to the public repo
drafts/

# Original Webflow export + rebuild reference material — source material for this
# migration, not the product itself; images get copied into public/ in Step 7 and
# don't need to live twice in the repo.
juliepaik.webflow/
ix2-interactions.json
ix2-interactions.md
juliepaik-rebuild-scope.md
claude-code-rebuild-prompt.md
extract-webflow-interactions.js

# build and run notes
README_INTERNAL.md
/rebuild-base

# Next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem
proxy-local.ts

# Claude Code
.claude/

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# dependencies
/node_modules
